spring:
  datasource:
    url: jdbc:h2:mem:auth_test_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

eureka:
  client:
    enabled: false

jwt:
  secret: "test-secret-key-test-secret-key-test-secret-key"
  expiration: 3600000